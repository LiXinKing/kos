20150503 在centos的minicom软件上接受串口数据，出现了：
    a
     a
      a
       a
        a
         a
          a
           a
            a
打印了若干个a，每次换行都是不是重头开始的。研究发现，'\n'字符只用于换行，
如果是回车的话要而外加入'\r'字符才行（回车字符是返回一行的开头）
